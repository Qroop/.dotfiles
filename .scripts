pintos-run() {
  if [ -z "$1" ]; then
    echo "Usage: pintos-run <filename>"
    return 1
  fi

  pintos -p "~/Courses/tdiu16/tdiu16-labs/src/examples/$1" -a "$1" -v -k --fs-disk=2 -- -f -q "run $1"
}
